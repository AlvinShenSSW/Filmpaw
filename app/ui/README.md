# filmpaw-ui

React 19 + MUI 7 + TanStack Router/Query, built with rsbuild.

```bash
pnpm install
pnpm dev          # http://localhost:3000 (expects server on 8720 outside Tauri)
pnpm typecheck
pnpm ci           # biome
pnpm build
```

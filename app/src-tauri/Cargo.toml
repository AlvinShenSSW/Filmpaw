[package]
name = "filmpaw"
version = "0.2.0"
description = "Filmpaw — NAS performer index + archive compare"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"

[profile.release]
strip = true
lto = true
